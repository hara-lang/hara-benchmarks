SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
BENCHMARK_ROOT=$(CDPATH= cd -- "$SCRIPT_DIR/../.." && pwd)
WORKSPACE_ROOT=${HARA_WORKSPACE_ROOT:-$(CDPATH= cd -- "$BENCHMARK_ROOT/../.." && pwd)}
HARA_ROOT="$WORKSPACE_ROOT/technology/hara"
HARA_CORE_ROOT="$HARA_ROOT/core"
ROOT="$HARA_ROOT"
export BENCHMARK_ROOT HARA_BENCHMARK_ROOT="$BENCHMARK_ROOT" WORKSPACE_ROOT HARA_ROOT HARA_CORE_ROOT
