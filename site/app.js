const $=id=>document.getElementById(id);
const state={runs:[],catalog:null,run:null,category:'Overall',language:'hara'};
const median=a=>{if(!a?.length)return null;const b=[...a].sort((x,y)=>x-y),m=Math.floor(b.length/2);return b.length%2?b[m]:(b[m-1]+b[m])/2};
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const runtimeBase=id=>id.startsWith('hara-')?'hara':id.split('-')[0];
const pretty=id=>id.replace('hara-rust-','Hara ').replaceAll('-',' ');
const platform=run=>[run.environment?.os||run.environment?.platform||'unknown',run.environment?.architecture||run.environment?.machine||''].filter(Boolean).join(' / ');
const steady=row=>row?.status==='ok'?median(row.steady_state?.samples_ns):null;
const format=(n,kind='time')=>{if(n==null)return 'not recorded';if(kind==='bytes')return n>=1048576?(n/1048576).toFixed(2)+' MiB':(n/1024).toFixed(1)+' KiB';const u=$('units')?.value||'auto';if(u==='ms')return(n/1e6).toFixed(3)+' ms';if(u==='us')return(n/1e3).toFixed(2)+' µs';if(u==='ns')return Math.round(n)+' ns';return n>=1e9?(n/1e9).toFixed(2)+' s':n>=1e6?(n/1e6).toFixed(3)+' ms':n>=1e3?(n/1e3).toFixed(2)+' µs':Math.round(n)+' ns'};
const option=(value,label=value)=>`<option value="${esc(value)}">${esc(label)}</option>`;
function params(){return new URLSearchParams(location.search)}
function remember(){const p=params();p.set('view',document.querySelector('.tabs [aria-selected=true]').dataset.view);p.set('run',$('run').value);p.set('units',$('units').value);p.set('display',$('display').value);p.set('category',state.category);p.set('workload',$('code-workload').value);p.set('language',state.language);history.replaceState(null,'',`${location.pathname}?${p}`)}
function selectedRows(){return state.run?.measurements?.filter(r=>(r.mode||'prepared')==='prepared')||[]}
function workloads(){return state.catalog.workloads}
function initSelectors(){const p=params();const unique=[...new Map(state.runs.map(r=>[r.run.id,r])).values()].sort((a,b)=>String(b.environment?.timestamp).localeCompare(String(a.environment?.timestamp)));state.runs=unique;$('run').innerHTML=unique.map(r=>option(r.run.id,`${r.run.profile} · ${new Date(r.environment.timestamp).toLocaleDateString()}${r.provenance?.hara_dirty?' · dirty':''}`)).join('');$('run').value=unique.some(r=>r.run.id===p.get('run'))?p.get('run'):unique[0]?.run.id;state.run=unique.find(r=>r.run.id===$('run').value);$('platform').innerHTML=option(platform(state.run));for(const id of ['lifecycle-workload','code-workload']){$(id).innerHTML=workloads().map(w=>option(w.id,`${w.id} · ${w.category}`)).join('')}if(p.get('workload')&&workloads().some(w=>w.id===p.get('workload'))){$('code-workload').value=p.get('workload');$('lifecycle-workload').value=p.get('workload')}$('units').value=p.get('units')||'auto';$('display').value=p.get('display')||'absolute';state.category=p.get('category')||'Overall';state.language=p.get('language')||'hara'}
function renderSummary(){const r=state.run,ok=selectedRows().filter(x=>x.status==='ok').length;$('run-summary').textContent=`${r.run.profile} profile · ${platform(r)} · ${ok}/${selectedRows().length} verified results${r.provenance?.hara_dirty?' · source checkout was dirty':''}`}
function renderMatrix(){const rows=selectedRows(),runtimes=[...new Set(rows.map(x=>x.runtime))],idx=new Map(rows.map(x=>[`${x.workload}|${x.runtime}`,x])),relative=$('display').value==='relative';let html='<table><thead><tr><th>Workload / category</th>'+runtimes.map(r=>`<th>${esc(pretty(r))}</th>`).join('')+'</tr></thead><tbody>';for(const workload of workloads()){const values=runtimes.map(r=>steady(idx.get(`${workload.id}|${r}`))),valid=values.filter(v=>v!=null),best=valid.length?Math.min(...valid):null,ordered=[...valid].sort((a,b)=>a-b);html+=`<tr><td><strong>${esc(workload.id)}</strong><div class="meta">${esc(workload.category)}</div></td>`;values.forEach((v,i)=>{const row=idx.get(`${workload.id}|${runtimes[i]}`);if(v==null){html+=`<td class="unsupported" title="${esc(row?.reason||'not measured')}">—<span class="cell-rank">unsupported</span></td>`;return}const rank=ordered.indexOf(v)+1,text=relative?(v/best).toFixed(2)+'×':format(v);html+=`<td class="${v===best?'fastest':''}" title="${esc(row.steady_state.samples_ns.length)} raw samples">${text}<span class="cell-rank">#${rank}</span></td>`});html+='</tr>'}html+='</tbody></table>';$('matrix-content').innerHTML=html}
function categories(){return ['Overall',...new Set(workloads().map(w=>w.category))]}
function calculateRanking(category){const all=workloads().filter(w=>category==='Overall'||w.category===category).map(w=>w.id),rows=selectedRows(),runtimes=[...new Set(rows.map(x=>x.runtime))],idx=new Map(rows.map(x=>[`${x.runtime}|${x.workload}`,x])),common=all.filter(w=>runtimes.every(r=>steady(idx.get(`${r}|${w}`))!=null)),fastest=Object.fromEntries(common.map(w=>[w,Math.min(...runtimes.map(r=>steady(idx.get(`${r}|${w}`))))]));return runtimes.map(runtime=>{const ratios=common.map(w=>steady(idx.get(`${runtime}|${w}`))/fastest[w]),score=ratios.length?Math.exp(ratios.reduce((s,v)=>s+Math.log(v),0)/ratios.length):null,supported=all.filter(w=>steady(idx.get(`${runtime}|${w}`))!=null).length,wins=ratios.filter(v=>Math.abs(v-1)<1e-9).length;return{runtime,score,supported,total:all.length,wins,common}}).sort((a,b)=>(a.score??Infinity)-(b.score??Infinity))}
function renderRankings(){const cats=categories();if(!cats.includes(state.category))state.category='Overall';$('category-tabs').innerHTML=cats.map(c=>`<button class="${c===state.category?'active':''}" data-category="${esc(c)}">${esc(c)}</button>`).join('');const ranked=calculateRanking(state.category),max=Math.max(...ranked.map(r=>r.score||0),1),common=ranked[0]?.common||[];$('ranking-content').innerHTML=`<p class="meta">Common scored workloads: ${esc(common.join(', ')||'none')}. Coverage is against ${state.category==='Overall'?'the full corpus':'this category'}.</p>`+ranked.map((r,i)=>`<article class="ranking"><div class="rank-number">${i+1}</div><div><strong>${esc(pretty(r.runtime))}</strong><div class="meta">${r.supported}/${r.total} coverage · ${r.wins} win${r.wins===1?'':'s'}</div></div><div class="bar"><i style="width:${r.score?Math.max(3,r.score/max*100):0}%"></i></div><div><strong>${r.score?r.score.toFixed(2)+'× fastest':'unranked'}</strong></div></article>`).join('');document.querySelectorAll('[data-category]').forEach(b=>b.onclick=()=>{state.category=b.dataset.category;renderRankings();remember()})}
function phaseValue(row,phase){if(row.status!=='ok')return null;if(phase==='steady')return steady(row);if(phase==='warmup')return median(row.warmup_samples_ns);return row[phase]??(phase==='cold_total_ns'?row.cold_start_ns:null)}
function spark(values){if(!values?.length)return'';const max=Math.max(...values),min=Math.min(...values),span=max-min||1,points=values.map((v,i)=>`${i/(values.length-1||1)*100},${38-(v-min)/span*32}`).join(' ');return `<svg viewBox="0 0 100 42" preserveAspectRatio="none" aria-label="warm-up samples"><polyline fill="none" stroke="#63e6a5" stroke-width="2" points="${points}"/></svg>`}
function renderLifecycle(){const workload=$('lifecycle-workload').value,phase=$('phase').value,rows=selectedRows().filter(r=>r.workload===workload),values=rows.map(r=>phaseValue(r,phase)),max=Math.max(...values.filter(v=>v!=null),1),bytes=phase.includes('bytes');$('lifecycle-content').innerHTML='<div class="timeline">'+rows.sort((a,b)=>(phaseValue(a,phase)??Infinity)-(phaseValue(b,phase)??Infinity)).map(row=>{const v=phaseValue(row,phase);return `<article class="row"><div><strong>${esc(pretty(row.runtime))}</strong><div class="meta">${phase==='prepare_ns'?(v==null?'Method documented in Code & harness':'measured in process'):row.status}</div></div><div class="bar">${phase==='warmup'?spark(row.warmup_samples_ns):`<i style="width:${v==null?0:Math.max(2,v/max*100)}%"></i>`}</div><div>${v==null?'not recorded':format(v,bytes?'bytes':'time')}</div></article>`}).join('')+'</div>'}
function renderCode(){const workload=workloads().find(w=>w.id===$('code-workload').value),languages=Object.keys(workload.implementations);if(!languages.includes(state.language))state.language=languages[0];$('language-tabs').innerHTML=languages.map(id=>`<button class="${id===state.language?'active':''}" data-language="${id}">${esc(workload.implementations[id].language)} · ${id}</button>`).join('');const impl=workload.implementations[state.language];$('code-content').innerHTML=`<div class="code-layout"><article class="code-block"><h3>Workload source <button class="copy" data-copy="source">Copy</button></h3><pre id="source-code"></pre></article><aside class="detail"><h3>Preparation</h3><dl><dt>Category</dt><dd>${esc(workload.category)}</dd><dt>Expected checksum</dt><dd>${esc(workload.expected)}</dd><dt>Operations</dt><dd>${workload.operations?.toLocaleString()||'not specified'}</dd><dt>Steps</dt><dd>${esc(impl.prepare.description)}</dd><dt>Command</dt><dd><code>${esc(impl.prepare.command)}</code></dd><dt>Harness</dt><dd>${esc(impl.harness_path)}</dd></dl></aside></div><article class="code-block" style="margin-top:1rem"><h3>Full runner harness <button class="copy" data-copy="harness">Copy</button></h3><pre id="harness-code"></pre></article>`;$('source-code').textContent=impl.source;$('harness-code').textContent=impl.harness;document.querySelectorAll('[data-language]').forEach(b=>b.onclick=()=>{state.language=b.dataset.language;renderCode();remember()});document.querySelectorAll('[data-copy]').forEach(b=>b.onclick=async()=>{await navigator.clipboard.writeText(b.dataset.copy==='source'?impl.source:impl.harness);b.textContent='Copied'})}
function renderRuns(){const r=state.run,env=r.environment||{},prov=r.provenance||{},versions=r.versions||{};const cards=[['Run ID',r.run.id],['Profile',r.run.profile],['Platform',platform(r)],['Timestamp',env.timestamp],['Hara revision',prov.hara_revision||env.git_revision],['Working tree',prov.hara_dirty??env.git_dirty?'dirty':'clean'],['Python coordinator',env.python]];$('runs-content').innerHTML='<div class="cards">'+cards.map(([k,v])=>`<div class="card"><span class="meta">${esc(k)}</span><strong>${esc(v??'not recorded')}</strong></div>`).join('')+'</div><h3>Runtime and compiler versions</h3><div class="version-grid">'+Object.entries(versions).map(([k,v])=>`<div class="card"><strong>${esc(k)}</strong><code>${esc(v)}</code></div>`).join('')+'</div>'+(Object.keys(versions).length<6?'<p class="warning">This historical run predates complete per-runtime version capture. Missing versions are not inferred.</p>':'')}
function renderAll(){state.run=state.runs.find(r=>r.run.id===$('run').value)||state.runs[0];renderSummary();renderMatrix();renderRankings();renderLifecycle();renderCode();renderRuns();remember()}
function activate(view){document.querySelectorAll('.view').forEach(v=>v.classList.toggle('active',v.id===view));document.querySelectorAll('.tabs [role=tab]').forEach(b=>b.setAttribute('aria-selected',String(b.dataset.view===view)));remember()}
function wire(){document.querySelectorAll('.tabs [role=tab]').forEach((b,i,all)=>{b.onclick=()=>activate(b.dataset.view);b.onkeydown=e=>{if(!['ArrowLeft','ArrowRight'].includes(e.key))return;const n=(i+(e.key==='ArrowRight'?1:-1)+all.length)%all.length;all[n].focus();activate(all[n].dataset.view)}});for(const id of ['run','units','display'])$(id).onchange=renderAll;$('lifecycle-workload').onchange=()=>{renderLifecycle();remember()};$('phase').onchange=()=>{renderLifecycle();remember()};$('code-workload').onchange=()=>{renderCode();remember()}}
Promise.all([fetch('data/runs.json').then(r=>r.json()),fetch('data/catalog.json').then(r=>r.json())]).then(([results,catalog])=>{state.runs=results.runs;state.catalog=catalog;initSelectors();wire();activate(params().get('view')||'matrix');renderAll()}).catch(error=>$('run-summary').textContent='Could not load benchmark evidence: '+error);
