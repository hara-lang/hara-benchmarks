const state={rows:[],filters:{}};
const $=id=>document.getElementById(id);
const median=a=>{if(!a?.length)return null;const b=[...a].sort((x,y)=>x-y),m=Math.floor(b.length/2);return b.length%2?b[m]:(b[m-1]+b[m])/2};
const platform=r=>[r.environment?.os||r.environment?.platform||'unknown',r.environment?.architecture||r.environment?.machine||''].filter(Boolean).join(' / ');
function value(row,metric){if(row.status!=='ok')return null;if(metric==='cold')return row.cold_start_ns;if(metric==='first')return row.first_call_ns;if(metric==='memory')return row.peak_rss_bytes;if(metric==='size')return row.artifact_bytes;return median(row.steady_state?.samples_ns)}
function format(n,metric){if(n==null)return 'not measured';if(metric==='memory'||metric==='size')return n>1048576?(n/1048576).toFixed(2)+' MiB':(n/1024).toFixed(1)+' KiB';return n>=1e9?(n/1e9).toFixed(2)+' s':n>=1e6?(n/1e6).toFixed(3)+' ms':n>=1e3?(n/1e3).toFixed(2)+' µs':Math.round(n)+' ns'}
function options(id,values){const node=$(id),current=node.value;[...new Set(values)].sort().forEach(v=>{const o=document.createElement('option');o.value=o.textContent=v;node.append(o)});node.value=current}
function render(){const metric=$('metric').value;let rows=state.rows.filter(x=>(!$('suite').value||x.suite===$('suite').value)&&(!$('workload').value||x.workload===$('workload').value)&&(!$('platform').value||x.platform===$('platform').value));const vals=rows.map(x=>value(x,metric)).filter(x=>x!=null),best=Math.min(...vals),worst=Math.max(...vals);rows.sort((a,b)=>(value(a,metric)??Infinity)-(value(b,metric)??Infinity));$('summary').textContent=`${rows.length} measurements · ${new Set(rows.map(x=>x.runtime)).size} runtimes · lower is better`;$('results').replaceChildren(...rows.map(x=>{const v=value(x,metric),ratio=v!=null&&best? v/best:null,width=v!=null?Math.max(2,100*v/(worst||v)):0,d=document.createElement('article');d.className='row'+(v==null?' unsupported':'');d.innerHTML=`<div><strong>${x.runtime}</strong><div class="meta">${x.workload} · ${x.platform}</div></div><div class="bar"><i style="width:${width}%"></i></div><div class="value">${format(v,metric)}${ratio?`<div class="meta">${ratio.toFixed(2)}× fastest</div>`:''}</div>`;return d}))}
fetch('data/runs.json').then(r=>r.json()).then(({runs})=>{state.rows=runs.flatMap(run=>run.measurements.map(row=>({...row,platform:platform(run)})));options('suite',state.rows.map(x=>x.suite));options('workload',state.rows.map(x=>x.workload));options('platform',state.rows.map(x=>x.platform));document.querySelectorAll('select').forEach(x=>x.addEventListener('change',render));render()}).catch(e=>$('summary').textContent='Could not load benchmark data: '+e);

